{
  "version": 2,
  "installCommand": "npm install && npm install --prefix server",
  "buildCommand": "npm run build",
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 30,
      "includeFiles": "server/**"
    }
  }
}
